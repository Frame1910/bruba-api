BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[invites] ADD [visits] INT NOT NULL CONSTRAINT [invites_visits_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
